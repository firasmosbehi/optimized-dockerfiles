# ❌ Non-Optimized SvelteKit Dockerfile
FROM node:20

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Build + run inside the same image
RUN npm run build

EXPOSE 3000
CMD ["npm", "run", "preview"]
